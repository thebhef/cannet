[package]
name = "cannet-sidecar"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

# Deliberately dependency-free: everything here is `std` process
# handling and line parsing, and both hosts (the GUI and the server)
# already pull their own async runtimes in. Nothing to add.
[dependencies]

[lints]
workspace = true
